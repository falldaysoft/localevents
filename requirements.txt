django>=6,<7
dj-database-url>=2.3
python-dotenv>=1.0
gunicorn>=23.0
psycopg[binary]>=3.2
whitenoise>=6.8

# Accounts. Email is the login identifier; mfa provides passkeys.
django-allauth[mfa]>=65.0

# Background tasks. Django 6 ships the `django.tasks` API; this package
# supplies the DatabaseBackend and the `db_worker` management command.
# Pinned <0.12 because 0.12 dropped the database backend module.
django-tasks[database]>=0.11,<0.12

# Pages and media. Pillow normalises uploads; markdown and nh3 turn a page's
# source into HTML that is safe to serve — nh3 rather than bleach, which is
# deprecated and unmaintained.
pillow>=11.0
markdown>=3.7
nh3>=0.2

# Enrichment: fetching, parsing, and geocoding.
httpx>=0.27
beautifulsoup4>=4.12
icalendar>=6.0
pydantic>=2.0

# LLM client. Lazy-imported, and pointed at any OpenAI-compatible endpoint —
# OpenRouter by default. Configured in the admin, not here.
openai>=1.0

pytest>=8.0
pytest-django>=4.9
